# Dependencies
node_modules/
**/node_modules/

# Environment variables (NEVER commit real secrets)
.env
.env.*
.env.local
.env.*.local
!.env.example

# Build outputs
dist/
build/
.next/
out/

# TypeScript incremental build cache — regenerated by every tsc/next build run,
# so it produces constant diff noise and must not be committed.
*.tsbuildinfo

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Testing
coverage/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Database
*.sqlite
*.sqlite3

# Uploaded/local media (use cloud storage in prod, not git)
/backend/uploads/
/backend/tmp/

# Prisma
/database/migrations/dev.db

# Deployment
.vercel
.turbo

# AI assistant config & personal notes — local only, never pushed.
# (Already-tracked copies were removed from the index with `git rm --cached`.)
.claude/
CLAUDE.md
**/CLAUDE.md
CLAUDE.local.md
AI_INSTRUCTIONS.md
RULES.md
.cursor/
.cursorrules
.github/copilot-instructions.md
AGENTS.md

# Personal learning / interview-prep notes
learning/
docs/learning/
*LEARNING*.md
*INTERVIEW*.md
*REVISION*.md
