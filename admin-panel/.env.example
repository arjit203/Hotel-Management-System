NEXT_PUBLIC_API_BASE_URL=http://localhost:5000/api/v1
NEXT_PUBLIC_ADMIN_PANEL_URL=http://localhost:3001
# Public website origin. Used only for the "View public page" links in the admin
# console (topbar and each property header). Optional — falls back to
# http://localhost:3000 when unset.
NEXT_PUBLIC_FRONTEND_URL=http://localhost:3000
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your_google_maps_api_key
